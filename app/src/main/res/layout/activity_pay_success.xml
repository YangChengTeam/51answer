<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include
        android:id="@+id/top_view"
        layout="@layout/common_top_view" />

    <ImageView
        android:id="@+id/iv_succeed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/t87dp"
        android:src="@mipmap/succeed"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/top_view" />

    <TextView
        android:id="@+id/tv_succeed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/t32dp"
        android:text="支付成功"
        android:textColor="@color/black_222"
        android:textSize="@dimen/t15sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/iv_succeed" />

    <TextView
        android:id="@+id/tv_sure"
        android:layout_width="match_parent"
        android:layout_height="@dimen/t40dp"
        android:layout_marginLeft="@dimen/t40dp"
        android:layout_marginTop="@dimen/t48dp"
        android:layout_marginRight="@dimen/t40dp"
        android:background="@drawable/red_rect_bg"
        android:gravity="center"
        android:text="@string/base_ok"
        android:textColor="@color/white"
        app:layout_constraintTop_toBottomOf="@id/tv_succeed" />
</androidx.constraintlayout.widget.ConstraintLayout>